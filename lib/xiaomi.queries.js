module.exports = {
    // Update id Module of Xiaomi_password Param
    updateIdParamPassword: `UPDATE param SET module = ? WHERE name="Xiaomi_password"`, 
  
    // Update id Module of Xiaomi_debug Param
    updateIdParamDebug: `UPDATE param SET module = ? WHERE name="Xiaomi_debug"`,
  
    // Update id Module of Xiaomi_debug_device Param
    updateIdParamDebugDevice: `UPDATE param SET module = ? WHERE name="Xiaomi_debug_device"`
  }